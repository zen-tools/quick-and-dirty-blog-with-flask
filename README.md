```
$ git clone https://github.com/zen-tools/quick-and-dirty-blog-with-flask.git
$ cd quick-and-dirty-blog-with-flask
$ virtualenv venv
$ source venv/bin/activate
(venv)$ pip install -r requirements.txt
(venv)$ ./run.py
```
